/** Host half for the browser-only conversation navigation plugin. */
export const name = 'dsh-conversation-nav'

/** No host services, events, tools, or persistence are required. */
export function apply() {}
